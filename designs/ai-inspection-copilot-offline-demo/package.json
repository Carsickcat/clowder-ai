{
  "name": "ai-inspection-copilot-offline-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/offline.browser.mjs",
    "check": "pnpm test && pnpm build && pnpm test:browser"
  },
  "devDependencies": {
    "playwright-core": "^1.62.0"
  }
}
